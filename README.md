# mixi2-ts

`tsdown` を使った TypeScript ビルド環境です。

## セットアップ

```bash
npm install
```

## コマンド

- `npm run build`: `dist/` に ESM/CJS と型定義を出力
- `npm run build:watch`: watch モードでビルド
- `npm run typecheck`: 型チェックのみ実行

## エントリポイント

- `src/index.ts`
