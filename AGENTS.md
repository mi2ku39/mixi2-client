# Codex Settings

このリポジトリで Codex が作業する際は、以下のルールに従うこと。

1. Pull Request のタイトルは英語で作成する。
2. Pull Request の説明・コメントは日本語で作成する。
3. ブランチ名は `codex/` で始め、後続は英語の kebab-case で命名する。`.` や `,` は使用しなくてよいブランチ名をつける。
   - 例: `codex/update-event-type-schema`
4. パッケージマネージャーは `pnpm` を使用する。
5. コミットメッセージはcConventional Commits 1.0.0に従って作成する
6. コミットメッセージは英語で作成する。
7. コミットメッセージは1行で、簡潔に記述する。
