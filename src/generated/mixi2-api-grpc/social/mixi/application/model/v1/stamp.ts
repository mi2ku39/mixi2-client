// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: social/mixi/application/model/v1/stamp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../../../google/protobuf/timestamp";
import { StampSetType, stampSetTypeFromJSON, stampSetTypeToJSON } from "../../const/v1/stamp_set_type";

/** 公式スタンプセットを表します。 */
export interface OfficialStampSet {
  /** スタンプセットの名前です。 */
  name: string;
  /** スタンプセットのスプライト画像のURLです。 */
  spriteUrl: string;
  /** スタンプセットに含まれるスタンプ一覧です。 */
  stamps: OfficialStamp[];
  /** スタンプセットIDです。 */
  stampSetId: string;
  /** スタンプセットが利用可能になる開始日時です。未指定の場合、開始日時は限定されません。 */
  startAt?:
    | Date
    | undefined;
  /** スタンプセットが利用可能でなくなる終了日時です。未指定の場合、終了日時は限定されません。 */
  endAt?:
    | Date
    | undefined;
  /** スタンプセットのタイプです。 */
  stampSetType: StampSetType;
}

/** 公式スタンプを表します。 */
export interface OfficialStamp {
  /** スタンプIDです。 */
  stampId: string;
  /** スタンプセット（スプライト）内での並び順です。 */
  index: number;
  /** スタンプの検索用タグの一覧です。 */
  searchTags: string[];
  /** スタンプの画像のURLです。 */
  url: string;
}

function createBaseOfficialStampSet(): OfficialStampSet {
  return { name: "", spriteUrl: "", stamps: [], stampSetId: "", startAt: undefined, endAt: undefined, stampSetType: 0 };
}

export const OfficialStampSet: MessageFns<OfficialStampSet> = {
  encode(message: OfficialStampSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.spriteUrl !== "") {
      writer.uint32(18).string(message.spriteUrl);
    }
    for (const v of message.stamps) {
      OfficialStamp.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.stampSetId !== "") {
      writer.uint32(34).string(message.stampSetId);
    }
    if (message.startAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startAt), writer.uint32(42).fork()).join();
    }
    if (message.endAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endAt), writer.uint32(50).fork()).join();
    }
    if (message.stampSetType !== 0) {
      writer.uint32(56).int32(message.stampSetType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OfficialStampSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOfficialStampSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.spriteUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stamps.push(OfficialStamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.stampSetId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.stampSetType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OfficialStampSet {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      spriteUrl: isSet(object.spriteUrl)
        ? globalThis.String(object.spriteUrl)
        : isSet(object.sprite_url)
        ? globalThis.String(object.sprite_url)
        : "",
      stamps: globalThis.Array.isArray(object?.stamps) ? object.stamps.map((e: any) => OfficialStamp.fromJSON(e)) : [],
      stampSetId: isSet(object.stampSetId)
        ? globalThis.String(object.stampSetId)
        : isSet(object.stamp_set_id)
        ? globalThis.String(object.stamp_set_id)
        : "",
      startAt: isSet(object.startAt)
        ? fromJsonTimestamp(object.startAt)
        : isSet(object.start_at)
        ? fromJsonTimestamp(object.start_at)
        : undefined,
      endAt: isSet(object.endAt)
        ? fromJsonTimestamp(object.endAt)
        : isSet(object.end_at)
        ? fromJsonTimestamp(object.end_at)
        : undefined,
      stampSetType: isSet(object.stampSetType)
        ? stampSetTypeFromJSON(object.stampSetType)
        : isSet(object.stamp_set_type)
        ? stampSetTypeFromJSON(object.stamp_set_type)
        : 0,
    };
  },

  toJSON(message: OfficialStampSet): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.spriteUrl !== "") {
      obj.spriteUrl = message.spriteUrl;
    }
    if (message.stamps?.length) {
      obj.stamps = message.stamps.map((e) => OfficialStamp.toJSON(e));
    }
    if (message.stampSetId !== "") {
      obj.stampSetId = message.stampSetId;
    }
    if (message.startAt !== undefined) {
      obj.startAt = message.startAt.toISOString();
    }
    if (message.endAt !== undefined) {
      obj.endAt = message.endAt.toISOString();
    }
    if (message.stampSetType !== 0) {
      obj.stampSetType = stampSetTypeToJSON(message.stampSetType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OfficialStampSet>, I>>(base?: I): OfficialStampSet {
    return OfficialStampSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OfficialStampSet>, I>>(object: I): OfficialStampSet {
    const message = createBaseOfficialStampSet();
    message.name = object.name ?? "";
    message.spriteUrl = object.spriteUrl ?? "";
    message.stamps = object.stamps?.map((e) => OfficialStamp.fromPartial(e)) || [];
    message.stampSetId = object.stampSetId ?? "";
    message.startAt = object.startAt ?? undefined;
    message.endAt = object.endAt ?? undefined;
    message.stampSetType = object.stampSetType ?? 0;
    return message;
  },
};

function createBaseOfficialStamp(): OfficialStamp {
  return { stampId: "", index: 0, searchTags: [], url: "" };
}

export const OfficialStamp: MessageFns<OfficialStamp> = {
  encode(message: OfficialStamp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stampId !== "") {
      writer.uint32(10).string(message.stampId);
    }
    if (message.index !== 0) {
      writer.uint32(16).uint32(message.index);
    }
    for (const v of message.searchTags) {
      writer.uint32(26).string(v!);
    }
    if (message.url !== "") {
      writer.uint32(34).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OfficialStamp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOfficialStamp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stampId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.searchTags.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OfficialStamp {
    return {
      stampId: isSet(object.stampId)
        ? globalThis.String(object.stampId)
        : isSet(object.stamp_id)
        ? globalThis.String(object.stamp_id)
        : "",
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      searchTags: globalThis.Array.isArray(object?.searchTags)
        ? object.searchTags.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.search_tags)
        ? object.search_tags.map((e: any) => globalThis.String(e))
        : [],
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: OfficialStamp): unknown {
    const obj: any = {};
    if (message.stampId !== "") {
      obj.stampId = message.stampId;
    }
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.searchTags?.length) {
      obj.searchTags = message.searchTags;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OfficialStamp>, I>>(base?: I): OfficialStamp {
    return OfficialStamp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OfficialStamp>, I>>(object: I): OfficialStamp {
    const message = createBaseOfficialStamp();
    message.stampId = object.stampId ?? "";
    message.index = object.index ?? 0;
    message.searchTags = object.searchTags?.map((e) => e) || [];
    message.url = object.url ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
